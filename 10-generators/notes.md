# Generators

Create a range of data over time...

Similar to JS version and uses `yield` and `next`

Can be used to create a list without storing the whole thing in memory

Much faster than creating the entire list
